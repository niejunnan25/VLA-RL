env:
  _target_: vla_rl.envs.libero.LiberoRemoteEnvBackend
  url: http://127.0.0.1:23000
  task_suite_name: libero_spatial
  task_id: 4
  action_dim: 7
  resolution: 256
  num_steps_wait: 10
  max_episode_steps: null
  benchmark_root: null
  libero_root: /vla/users/niejunnan/codebase/serl_torch/third_party/LIBERO
  libero_config_dir: null
  libero_datasets_root: /vla/users/niejunnan/datasets
  seed: 0
  image_size: 224
  timeout: 120.0

policy:
  _target_: vla_rl.policies.openpi.OpenPIBackend
  openpi_root: /vla/users/niejunnan/codebase/openpi-rlt-github
  config_name: pi05_libero
  checkpoint_path: /vla/users/niejunnan/assets/openpi-assets/checkpoints/pi05_libero_pytorch
  action_dim: 7
  device: cuda

algorithm:
  _target_: vla_rl.algorithms.fake.FakeAlgorithm
  action_dim: 7
  chunk_size: 4
  seed: 2

runtime:
  _target_: vla_rl.runtime.local_actor_learner.LocalActorLearnerRunner
  max_env_steps: 1000
  max_update_steps: 1000
  warmup_steps: 0
  execute_horizon: 1
  batch_size: 1
  replay_capacity: 100000
  gamma: 0.99
  metrics_path: outputs/libero_spatial_openpi_fake_algorithm/metrics.jsonl
  replay_seed: 0
