env:
  _target_: vla_rl.envs.fake.FakeEnvBackend
  action_dim: 7
  proprio_dim: 8
  max_steps: 10
  image_shape: [32, 32, 3]
  seed: 0

policy:
  _target_: vla_rl.policies.fake.FakePolicyBackend
  action_dim: 7
  chunk_size: 4
  embedding_dim: 16
  seed: 1

algorithm:
  _target_: vla_rl.algorithms.fake.FakeAlgorithm
  action_dim: 7
  chunk_size: 4
  seed: 2

runtime:
  _target_: examples.fake_debug.local_runner.LocalRunner
  max_steps: 10
  gamma: 0.99
  metrics_path: outputs/fake_local/metrics.jsonl
